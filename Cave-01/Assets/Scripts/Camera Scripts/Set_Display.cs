using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Set_Display : MonoBehaviour
{
	public string[] Text = new string[5];

	public static int MyScreenWidth = 1542, MyScreenHeight = 753;

	IEnumerator Start()
	{
		// 1. ??????????????Display 0??
		int mainWidth = 1920;// 1542;//1285
		int mainHeight = 1080;// 753;//628
		//Display.displays[0].Activate(mainWidth, mainHeight, 60); // ?Display 0????????
		Screen.SetResolution(mainWidth, mainHeight, FullScreenMode.Windowed); // ?????????????
		Text[0] = $"???????????{mainWidth}x{mainHeight}";

		// 2. ??????????????????Display 1??
		float timeout = 5f;
		float elapsed = 0;
		bool secondDisplayReady = false;

		while (elapsed < timeout)
		{
			if (Display.displays.Length > 1)
			{
				// ????????????????
				if (Display.displays[1].systemWidth > 0)
				{
					secondDisplayReady = true;
					break;
				}
			}
			elapsed += Time.deltaTime;
			yield return null;
		}

		if (secondDisplayReady)
		{
			// 3. ???????????????????????????1920x1080??
			int secondWidth = Display.displays[1].systemWidth;
			int secondHeight = Display.displays[1].systemHeight;
			//display.Activate(secondWidth, secondHeight, 60); // ????????????��????

			Display.displays[1].Activate();

			Text[1] = $"????????????{secondWidth}x{secondHeight}";
		}
		else
		{
			Text[3] = "��???????????";
		}
	}
}